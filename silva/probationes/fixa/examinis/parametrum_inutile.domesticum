/* parametrum inutile (codex 70) - paritas -Wunused-parameter
 * (in -Wextra); (void)x usus est.
 * ORACULUM -Wunused-parameter */

/* EXSPECTA-PROXIMA: PARAMETRUM_INUTILE */
static int f(int usatus, int otiosus)
{
    return usatus + f(usatus, usatus);
}
