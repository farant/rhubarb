RHUBARB CODEBASE MAP
====================
Last updated: 2026-07-24

A C89 monorepo for building catholic civilization.
All identifiers in Latin. No dependencies. Long-term thinking.


DIRECTORY STRUCTURE
-------------------

rhubarb/
├── include/         120 header files (.h)
├── lib/             123 implementation files (.c, .m)
├── probationes/     113 test modules (probatio_*.c)
├── tools/           15 utility executables (.c/.m → bin/)
│
│   ── SELF-CONTAINED SUBPROJECTS (own fontes/ + probationes/ +
│      compile_probationes.sh + phase-log.md; most amalgamate to
│      amalgama/{name.c,name.h}; START AT each one's phase-log.md) ──
│
├── silva/           C89 parser + static-analysis frontend, 4th gen.
│                    Supersedes arbor/arbor2/lapifex. Now far past the
│                    parser: semantica (name resolution/types) + the
│                    `examen` C89 judgment tiers (clang-parity lint) +
│                    CFG/dataflow (fluxus). Shell bench: nexus.sh,
│                    examen.sh, censor.sh, differentia.sh, emitte.sh.
│                    START AT silva/CLAUDE.md + silva/phase-log.md.
├── officina/        Compiler / VM / debugger toolchain over the silva
│                    frontend. Pipeline: fons → silva → semantica →
│                    demissio → conexio → machinula → indicium → vindex.
│                    LSP server (legatus.sh -mcp; legatus-plugin/) + REPL
│                    (colloquium.sh) + debugger TUI (vindex.sh).
│                    START AT officina/CLAUDE.md + officina/phase-log.md.
├── tessera/         ANSI-terminal TUI library, anti-ncurses (xterm-only,
│                    zero terminfo). deliverable = amalgama/{tessera.c,
│                    tessera.h}; demo: ./tessera/spectaculum.sh; powers
│                    saltuarius.
├── saltuarius/      TUI code explorer (fuzzy file/symbol bar) built on
│                    tessera. daily-driver. saltuarius.sh.
├── knotapel/        Knot-algebra research (Kauffman bracket, Jones,
│                    Temperley-Lieb, DKC). 100+ numbered demo_NN_* dirs;
│                    scrutinium.h algebraic-analysis framework; atlas/.
├── gesta/           TABULARIUM records office: append-only JSONL ledger
│                    (annales/tabularium.jsonl = truth) behind the
│                    tabularii MCP server. cold path: ./gesta/frigida.sh.
│
│   ── SUPPORTING DIRS ──
│
├── project-specs/   design records (silva-spec*, tessera-*, etc.)
├── episodes/        Novum Fundamentum TV-show scripts (1 lib = 1 episode)
├── vim/             editor integration: Latin-C syntax + ftdetect
├── lint/            house lint probationes
├── instrumenta/     standalone code generators (nuntium_schema_generare)
├── docs/  reference/  study/  stml-parser-reference/   documentation & notes
├── data/  book_assets/  gutenberg-data/  output/       data & generated output
├── bin/  build/  vendor/  scripts/                     binaries, artifacts, 3rd-party, scripts
└── .claude/         claude code config (+ MEMORY.md auto-memory)


LIBRARY DEPENDENCY TIERS  (include/ + lib/ core libraries)
------------------------

    ┌─────────────────────────────────────────────────────────────┐
    │  TIER 0: FOUNDATION                                         │
    │  (no dependencies, used by everything)                      │
    │                                                             │
    │  latina.h ──── Latin macros for C keywords                  │
    │  piscina.h ─── Arena allocator (memory pools)               │
    │  credo.h ───── Test framework                               │
    │  chorda.h ──── String struct {mensura, datum} NOT null-term │
    │  xar.h ─────── Variable-length array                        │
    │  uuid.h ────── UUID generation                              │
    │  utf8.h ────── UTF-8 encoding                               │
    │  base64.h ──── Base64 encode/decode                         │
    │  sigillum.h ── Content seal (SHA-256, FIPS 180-4)           │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  TIER 1: DATA STRUCTURES & UTILITIES                        │
    │                                                             │
    │  ┌─ Strings ───────────┐  ┌─ Collections ─────────┐         │
    │  │ chorda_aedificator  │  │ tabula_dispersa (hash)│         │
    │  │ internamentum       │  │ friatio (hashing)     │         │
    │  │ similitudo (fzf)    │  │ moneta (ULID mint)    │         │
    │  └─────────────────────┘  └───────────────────────┘         │
    │  ┌─ Data Formats ──────┐  ┌─ Entity System ───────┐         │
    │  │ json, xml, toml     │  │ entitas               │         │
    │  │ stml, nuntium       │  │ entitas_repositorium  │         │
    │  │ nuntium_schema      │  │ persistentia          │         │
    │  └─────────────────────┘  └───────────────────────┘         │
    │  ┌─ Messaging ─────────┐  ┌─ Documents ───────────┐         │
    │  │ cursus (msg bus)    │  │ scrinium (doc archive)│         │
    │  │ tractator (handlers)│  │ tabellarius (JSON-RPC)│         │
    │  └─────────────────────┘  └───────────────────────┘         │
    │  ┌─ Utilities ─────────────────────────────────────┐        │
    │  │ uri, url, via (paths), tempus, fasti            │        │
    │  │ argumenta, sectio, symbola, iter_directoria     │        │
    │  │ exemplar (templates), quaerere (search)         │        │
    │  │ vigilia (resident-process watch)                │        │
    │  │ filum (file read/write, dir ops)                │        │
    │  │ processus (spawn+capture, argv vector, NO shell) │        │
    │  └─────────────────────────────────────────────────┘        │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  TIER 2: DOMAIN-SPECIFIC                                    │
    │                                                             │
    │  ┌─ ARBOR (C Parser) ──────────────────────────────────┐    │
    │  │                                                     │    │
    │  │  arbor_lexema ──► arbor_praeparator ──► arbor_syntaxis   │
    │  │       │                                      │      │    │
    │  │       └──────────────────┬────────────────────┘     │    │
    │  │                          ▼                          │    │
    │  │    arbor_quaestio    arbor_formator    arbor_index  │    │
    │  │    arbor_typus            │                         │    │
    │  │                          ▼                          │    │
    │  │                   arbor.h (facade)                  │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ ARBOR2 (GLR Parser Framework) ─────────────────────┐    │
    │  │  arbor2_lexema ──► arbor2_token ──► arbor2_glr      │    │
    │  │  arbor2_expandere (grammar expansion)               │    │
    │  │  arbor2_conditio_evaluare   arbor2_scribere (AST→src)│   │
    │  │  arbor2_lapifex_adaptator (bridge to lapifex)       │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ LAPIFEX (C89 parser gen) ──── frozen / reference ──┐    │
    │  │  lapifex_lexere ──► lapifex_parsere                 │    │
    │  │  lapifex_c89   lapifex_glr   lapifex_generare       │    │
    │  └─────────────────────────────────────────────────────┘    │
    │       (all of arbor/arbor2/lapifex superseded by silva/)    │
    │                                                             │
    │  ┌─ SPUTNIK (Custom Language) ─────────────────────────┐    │
    │  │  sputnik_lexema ──► sputnik_parser ──► sputnik_interpres │
    │  │                     sputnik_syntaxis (GUI)          │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ Graphics/Rendering ────────────────────────────────┐    │
    │  │  color, coloratio ─── color systems                 │    │
    │  │  fons, fons_visus, fons_6x8 ─── font rendering      │    │
    │  │  delineare ────────── 2D drawing primitives         │    │
    │  │  imago, imago_opus ── image processing              │    │
    │  │  dithering ────────── dithering algorithms          │    │
    │  │  cursor ───────────── custom mouse cursor           │    │
    │  │  tabula_characterum ─ 2D character grid (text edit) │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ UI Framework ──────────────────────────────────────┐    │
    │  │                                                     │    │
    │  │  fenestra (macOS native window)                     │    │
    │  │      │                                              │    │
    │  │      ▼                                              │    │
    │  │  widget ──► layout ──► pagina ──► schirmata         │    │
    │  │  elementa (immediate-mode GUI components)           │    │
    │  │      │                                              │    │
    │  │      ├── dialogus, dialogus_importatio (dialogs)    │    │
    │  │      ├── registrum_commandi (commands)              │    │
    │  │      ├── registrum_widget (widget registry)         │    │
    │  │      ├── selectio (selection)                       │    │
    │  │      ├── pinacotheca_visus, importatio_visus        │    │
    │  │      ├── calendario_visus (liturgical widget)       │    │
    │  │      ├── arx_caeli (spatial map/card editor)        │    │
    │  │      └── paginarium, sententia_* (pagination)       │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ Webview App Layer ─────────────────────────────────┐    │
    │  │  vitrea ──────── WKWebView over fenestra, serving   │    │
    │  │                  capsula:// assets; JS<->C bridge   │    │
    │  │                  (all WebKit in vitrea_macos.m)     │    │
    │  │  internuntius ── pure C89 JSON-RPC (registry,       │    │
    │  │                  envelope, push) over a missor      │    │
    │  │                  seam - transport-agnostic          │    │
    │  │  speculum ────── self-source debug mode (Cmd+Shift+D)│   │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ Domain Libraries ──────────────────────────────────┐    │
    │  │  biblia, biblia_visus ──── Bible text + rendering   │    │
    │  │  librarium_* ───────────── book library system      │    │
    │  │  libro_paginarum ───────── book pagination          │    │
    │  │  calendarium_liturgicum ── liturgical calendar      │    │
    │  │  sanctorale ────────────── saints calendar          │    │
    │  │  navigator_entitatum ───── entity navigation        │    │
    │  │  tempestivum ───────────── timer entities           │    │
    │  │  vim ───────────────────── vim keybindings          │    │
    │  │  thema, thema_visus ────── theming                  │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ Platform/Networking ───────────────────────────────┐    │
    │  │  clipboard_platform ─── macOS clipboard             │    │
    │  │  tcp, tls ───────────── networking (POSIX/macOS)    │    │
    │  │  http, multipart ────── HTTP client                 │    │
    │  │  router ─────────────── HTTP route matching         │    │
    │  │  servitium, reactor ─── server/event loop           │    │
    │  │  macho ──────────────── Mach-O binary format        │    │
    │  │  cliens_tabularii ───── tabulariumd client (MCP over │   │
    │  │                         TCP, start-if-absent)       │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ Build / Remote Administration ─────────────────────┐    │
    │  │  aedilis ──────── derivative build layer: include    │   │
    │  │                   closures, STML manifests, emitted  │   │
    │  │                   scripts (include/X.h -> lib/X.c)   │   │
    │  │  villa_lectores ─ PURE ssh command builder + parsers  │   │
    │  │                   (systemctl, nginx -T, df, meminfo);│    │
    │  │                   no network - fixture-driven        │    │
    │  └─────────────────────────────────────────────────────┘    │
    │                                                             │
    │  ┌─ Utilities ─────────────────────────────────────────┐    │
    │  │  capsula, capsula_caudae ── asset bundling          │    │
    │  │  actor ──────────────────── actor/message passing   │    │
    │  │  flatura ────────────────── compression (LZ77)      │    │
    │  │  concha ─────────────────── shell/REPL              │    │
    │  └─────────────────────────────────────────────────────┘    │
    └─────────────────────────────────────────────────────────────┘


TOOLS
-----

tools/
├── generare.c ─────────────────────── Run codegen directives pre-compile
├── glr_debug.c ─────────────────────── Debug GLR parser
├── glr_quaestio.c ─────────────────── Query GLR parser (clone)
├── sputnik.c ───────────────────────── Sputnik interpreter
├── genera_biblia.c ─────────────────── Generate Bible database
├── gutenberg_index.c ──────────────── Index Gutenberg books
├── gutenberg_census.c ─────────────── Analyze Gutenberg collection
├── lector_eventuum.c ──────────────── Event/log reader
├── librarium_collector.c ──────────── Collect books into library
├── librarium_merge.c ──────────────── Merge library files
├── capsula_generare.c ─────────────── Generate asset capsule
├── capsula_caudae_adiungere.c ─────── Add assets to capsule
├── vitrea_spica.m ─────────────────── WebKit calibration probe
│                                      (re-run after macOS updates)
├── fons_merge.c ───────────────────── Merge font files
└── bitsyfont_ad_fons.c ────────────── Convert bitmap font


KEY PATTERNS
------------

Memory:     piscina (arena) ──► all allocations tied to arena lifetime
                               mark/reset with PiscinaNotatio
                               no manual free()

Strings:    chorda = {i32 mensura; i8* datum}
            NOT null-terminated! Never use strcpy/strlen/etc.
            Use internamentum for deduplication

Data:       nuntium ──► protobuf-style binary (compact, typed)
            nuntium_schema ──► schema language for nuntium messages
            json/xml/toml/stml ──► text formats

Entity:     entitas ──► properties with typed values
            entitas_repositorium ──► event sourcing storage
            persistentia ──► memory or file backend

Records:    tabularium ──► append-only JSONL ledger (gesta/) behind the
                           tabularii MCP; the project board lives here,
                           not in prose


BUILD
-----

Compiler:   C89 strict
Flags:      -pedantic -Wall -Wextra -Werror -Wconversion
            -Wsign-conversion -Wcast-qual -Wstrict-prototypes
            -Wmissing-prototypes -Wwrite-strings

Scripts:    ./compile_tests.sh <pattern>  ── run tests matching pattern
            ./compile_tools.sh            ── compile all tools
            ./compile_library.sh          ── compile library
            (subprojects have their own ./compile_probationes.sh)


NOTES
-----

- All code in Latin (identifiers, comments, messages)
- "nomen" is a macro for typedef, don't use as variable name
  (every word #defined in latina.h is a reserved identifier)
- Platform: macOS only (platform layer exists for future porting)
- Tests use credo.h framework (see probationes/probatio_piscina.c)
- Worklogs: lib/foo.worklog.md for non-obvious decisions
- Subprojects (silva/officina/tessera/saltuarius/knotapel/gesta) are
  self-contained: read each one's phase-log.md + CLAUDE.md first
- The project ledger is the tabularii MCP, not this file; MAP.txt is a
  structural orientation map, the store is the live board
