# fasciculum plan 1 — the library (P1–P5 of fasciculum-spec.md)

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `include/fasciculum.h` + `lib/fasciculum.c` — describe a macOS
`.app` as a value, render it purely into a list of files, write that
list, read a bundle back, compare two — with the executable-bit gap
fixed in `filum` first, every gate born red, and the one question no
suite can answer (does it launch?) moved to a by-hand smoke.

**Architecture:** `Fasciculum` (value) → `fasciculum_reddere` (pure:
returns an `Xar` of `FasciculumPars`, each either generated bytes or a
source path, reading no file contents) → `fasciculum_scribere` (the only
I/O: mkdir, write, copy, chmod) → `fasciculum_legere` (inverse, refusing
with a named cause). The `Info.plist` is a `PlistValor` dict serialized
by `plist_scribere`, so this library writes no XML.

**Tech Stack:** C89 under the house flags; `plist`, `filum`, `via`,
`xar`, `chorda`, `piscina`, `credo`; `plutil` for plist validation
inside one gate; pythonica (`silva.commissio`, `silva.planta`,
`silva.Refactio`) for gates and commits.

**Spec:** `project-specs/fasciculum-spec.md` v1 — this plan implements
§3 (value and plan), §4 (generated plist), §5 (writing), §6 (refusals),
§7 (two `filum` rows), §8 (gates H1–H6) and §9 (phases P1–P5).

## Global Constraints

- **Compile flags, verbatim** (`tools/vexilla.sh`, never a local copy):
  `-std=c89 -pedantic -Wall -Wextra -Werror -Wconversion
  -Wsign-conversion -Wcast-qual -Wstrict-prototypes
  -Wmissing-prototypes -Wwrite-strings -Wno-long-long
  -Wno-overlength-strings -fbracket-depth=512 -O2 -g`
- **Latin identifiers, comments and messages.** English only in `.md`.
- **`latina.h` macros are FORBIDDEN identifiers.** This bit twice in
  plist's arc: `integer` expands to `int`, `nomen` to `typedef`. Also
  taken: `casus`, `magnitudo`, `structura`, `brevis`, `registrum`,
  `exire`; single capitals `I V X L C D M` are numerals.
- **C89 shape:** declarations at the head of each block; no `//`; no
  `_Bool`; no variadic macros; no flexible arrays.
- **`i32`/`i64` UNSIGNED, `s32`/`s64` signed;** guard any bound derived
  by subtraction. **`chorda` is NOT NUL-terminated** and may contain NUL.
- **New C file → `./silva/scribe.sh <via> <<'EOF'`** (write + examen in
  one call). **Existing C file → `silva.Editio` / `silva.Refactio`.**
- **The fontes generator runs AFTER the new library exists.**
  `./tools/compile_tests_fontes_generare.sh` derives `SOURCE_FILES` from
  `lib/`, so running it earlier reports "no change" and the link then
  fails on undefined symbols (plist plan, task 4).
- **Every gate is born red by a planted fault** (`silva.planta`). A
  plant must keep the code compiling AND every variable used — blinding
  a condition that sets a variable trips `-Wunused-but-set-variable` and
  the pre-flight refuses it; invert the condition instead.
- **Calibrations run SEQUENTIALLY, never batched**, and reverting a
  plant DELETES the built binary — two parallel calibrations raced over
  `lib/` and `build/` in plist's task 6 and cost three rounds of
  misdiagnosis on a planted binary that survived a source revert.
- **A refusal test asserts the SPECIFIC status**, plus the path it names.
- **Format before commit:** `./silva/formator.sh <viae> -scribere` then
  `-vitia`; lines ≤ 72 columns. Pick plant anchors AFTER formatting.
- **New identifier words:** `./oratio/vocabula.sh -nova` names them; the
  lint is a dictionary with gaps, not a proscription — add a glossary
  entry for a real term, rename only when the name itself is bad.
- **Commit from Python:** `silva.commissio(msg, viae, portae=[...])`.
- **Exit 2 from any runner means NOTHING RAN.**

---

## File structure

| File | Responsibility |
|---|---|
| `include/filum.h` (modify) | declare `filum_modum_ponere`; append `potest_exsequi` to `FilumStatus` |
| `lib/filum.c` (modify) | implement the mode setter; set the new field in `filum_status` |
| `probationes/probatio_filum.c` (modify) | task 1 gates |
| `lib/filum.worklog.md` (create) | the copy-drops-mode finding |
| `include/fasciculum.h` (create) | value, plan, statuses, four functions |
| `lib/fasciculum.c` (create) | render, write, read, compare |
| `probationes/probatio_fasciculum.c` (create) | H1–H6 |
| `tools/fasciculum_fumus.sh` (create) | by-hand: build a real bundle and launch it |
| `lib/fasciculum.worklog.md` (create) | findings at find-time |

---

### Task 1: `filum` — the mode pair

Implements spec §7.1 and §7.2. Nothing in task 3 can assert its own
work without this.

**Files:**
- Modify: `include/filum.h` (declare after `filum_directorium_existit`,
  `:195-198`; append the field to `FilumStatus`, `:205-211`)
- Modify: `lib/filum.c` (`filum_status` at `:1001-1037`)
- Modify: `probationes/probatio_filum.c` (insert before the
  `imprimere("\n"); credo_imprimere_compendium();` pair at the end)
- Create: `lib/filum.worklog.md`

**Interfaces:**
- Consumes: nothing.
- Produces: `b32 filum_modum_ponere(constans character* via, integer
  modus)`; `FilumStatus.potest_exsequi`.

- [ ] **Step 1: Write the failing tests**

Insert before the compendium pair in `probationes/probatio_filum.c`
(`piscina` is in scope; the file already includes `filum.h`, `piscina.h`,
`credo.h`, `<stdio.h>`, `<string.h>`):

```c
    /* ---- modus: ponere, et bit exsequendi in statu ---- */
    {
        constans character* via_modi =
            "/tmp/probatio_filum_modus.txt";
        constans character* via_copiae =
            "/tmp/probatio_filum_copia.txt";
                FilumStatus st;

        imprimere("\n--- Probans filum_modum_ponere ---\n");

        CREDO_VERUM(filum_scribere_literis(via_modi, "x"));
        CREDO_VERUM(filum_status(via_modi, &st));
        CREDO_FALSUM(st.potest_exsequi);

        CREDO_VERUM(filum_modum_ponere(via_modi, 0755));
        CREDO_VERUM(filum_status(via_modi, &st));
        CREDO_VERUM(st.potest_exsequi);
        CREDO_VERUM(st.potest_legere);
        CREDO_VERUM(st.potest_scribere);

        /* modus restrictus: bit exsequendi iterum abest */
        CREDO_VERUM(filum_modum_ponere(via_modi, 0644));
        CREDO_VERUM(filum_status(via_modi, &st));
        CREDO_FALSUM(st.potest_exsequi);

        /* argumenta mala RECUSANTUR, non tacent */
        CREDO_FALSUM(filum_modum_ponere(
            "/tmp/probatio_filum_nusquam_xyz", 0755));
        CREDO_FALSUM(filum_modum_ponere(NIHIL, 0755));

        /* CAUSA huius paris: copia modum NON servat */
        CREDO_VERUM(filum_modum_ponere(via_modi, 0755));
        CREDO_VERUM(filum_copiare(via_modi, via_copiae));
        CREDO_VERUM(filum_status(via_copiae, &st));
        CREDO_FALSUM(st.potest_exsequi);

        filum_delere(via_copiae);
        filum_delere(via_modi);
    }
```

- [ ] **Step 2: Run and watch it fail**

```bash
./compile_tests.sh filum > /tmp/f1.log 2>&1; echo "rc=$?"
grep -E 'error:' /tmp/f1.log | head -5
```

Expected: `no member named 'potest_exsequi'` and an implicit
declaration of `filum_modum_ponere`.

- [ ] **Step 3: Declare both**

In `include/filum.h`, after `filum_directorium_existit` (`:198`):

```c
/* Ponere modum fili (chmod)
 *
 * CUR EXSISTIT: filum_copiare ansa octetorum nuda est, ergo modum NON
 * servat - binarium in fasciculum copiatum non exsecutabile cadit, et
 * fasciculus sine bite +x duplici ictu sine diagnosi deficit.
 *
 * Reddit VERUM in successu; FALSUM si via NIHIL aut chmod deficit.
 */
b32
filum_modum_ponere (
    constans character* via,
               integer  modus);
```

And append the field to `FilumStatus` (never reorder the others):

```c
nomen structura {
    memoriae_index mensura;         /* Mensura in bytes */
               b32 est_directorium; /* Est directorium? */
               b32 est_filum;       /* Est filum regulare? */
               b32 potest_legere;   /* Legibile? */
               b32 potest_scribere; /* Scribabile? */
               b32 potest_exsequi;  /* Exsequibile? (bit +x usoris) */
} FilumStatus;
```

- [ ] **Step 4: Implement**

`lib/filum.c` already calls `stat` and `mkdir` from `<sys/stat.h>`, so
`chmod` comes from a header it already includes. If it proves hidden
under `-std=c89`, the house remedy is `postulata_posix.h` FIRST in the
file (codex 85) — do not work around it locally.

Append to `lib/filum.c`:

```c
b32
filum_modum_ponere (
    constans character* via,
               integer  modus)
{
    si (!via)
    {
        _filum_error_ponere("via est NIHIL");
        redde FALSUM;
    }

    _filum_error_purgare();

    si (chmod(via, (mode_t)modus) != ZEPHYRUM)
    {
        _filum_error_ponere("chmod fracta");
        redde FALSUM;
    }

    redde VERUM;
}
```

And in `filum_status`, beside the other flags in BOTH arms:

```c
#ifdef _WIN32
    status_out->potest_exsequi   = (st.st_mode & _S_IEXEC) != ZEPHYRUM;
#else
    status_out->potest_exsequi   = (st.st_mode & S_IXUSR) != ZEPHYRUM;
#endif
```

- [ ] **Step 5: Run and watch it pass**

```bash
./compile_tests.sh filum > /tmp/f1.log 2>&1; echo "rc=$?"
grep -E 'Totalis|Praeteriti|Fracti|Conditio' /tmp/f1.log
```

- [ ] **Step 6: Prove the gate can go red**

```bash
python3 - <<'PY'
import sys; sys.path.insert(0, 'pythonica'); import silva
print(silva.planta('lib/filum.c',
                   'si (chmod(via, (mode_t)modus) != ZEPHYRUM)',
                   'si (FALSUM && chmod(via, (mode_t)modus) != ZEPHYRUM)',
                   'radix', 'filum'))
PY
```

Expected RED on the `potest_exsequi` assertion (chmod never runs, so
the bit is never set), GREEN after revert.

- [ ] **Step 7: Whole suite, worklog, commit**

`filum` is included widely and `FilumStatus` grew a field, so the
filtered gate is not enough evidence:

```bash
./compile_tests.sh > /tmp/omnia.log 2>&1; echo "rc=$?"
grep -E 'Tests (Total|Passed|Failed)' /tmp/omnia.log
./silva/formator.sh include/filum.h lib/filum.c \
    probationes/probatio_filum.c -scribere
./silva/formator.sh include/filum.h lib/filum.c \
    probationes/probatio_filum.c -vitia
```

`lib/filum.worklog.md` records: `filum_copiare` is a byte loop and drops
mode; `FilumStatus` had no execute flag, so a test could not assert a
bit it had just set; both found while designing fasciculum, both useful
beyond bundles. Then:

```bash
python3 - <<'PY'
import sys; sys.path.insert(0, 'pythonica'); import silva
print(silva.commissio(
    'filum: par modi (filum_modum_ponere + potest_exsequi in '
    'FilumStatus) - filum_copiare modum non servat, ergo binarium in '
    'fasciculum copiatum non exsecutabile caderet; et status vexillum '
    'exsequendi non ferebat, ergo probatio bitem quem ipsa posuit '
    'asserere non poterat (fasciculum-spec par. VII)',
    ['include/filum.h', 'lib/filum.c', 'probationes/probatio_filum.c',
     'lib/filum.worklog.md'],
    portae=[('radix', 'filum')]))
PY
```

---

### Task 2: the value and the pure render

Implements spec §3, §4; gates H1 and H2. No filesystem is touched by
anything this task adds.

**Files:**
- Create: `include/fasciculum.h`, `lib/fasciculum.c`,
  `probationes/probatio_fasciculum.c`
- Run (AFTER `lib/fasciculum.c` exists):
  `./tools/compile_tests_fontes_generare.sh`

**Interfaces:**
- Consumes: task 1's `FilumStatus.potest_exsequi` (not yet asserted
  here), plist's whole surface (`include/plist.h`).
- Produces: `Fasciculum`, `FasciculumOrigo`, `FasciculumPars`,
  `FasciculumStatus`, `fasciculum_reddere`.

- [ ] **Step 1: Write `include/fasciculum.h`**

```bash
./silva/scribe.sh include/fasciculum.h <<'EOF'
/* fasciculum.h - Fasciculi macOS (.app): struere, legere, conferre.
 *
 * Fasciculus est directorium formae fixae: Foo.app/Contents/ cum
 * Info.plist, exsecutabili sub MacOS/, et rebus optionalibus. Haec
 * bibliotheca eum ut VALOREM describit, valorem in indicem plagularum
 * PURE reddit, indicem scribit, fasciculum exsistentem relegit, duos
 * confert.
 *
 * Info.plist dictio PlistValor est quam plist_scribere serializat -
 * hic nihil de XML scribitur. Specificatio:
 * project-specs/fasciculum-spec.md v1.
 *
 * NON tractat: subscriptionem, notarizationem, Launch Services,
 * .framework / .bundle / .appex, generationem iconum (id icones erit).
 */

#ifndef FASCICULUM_H
#define FASCICULUM_H

#include "latina.h"
#include "chorda.h"
#include "piscina.h"
#include "internamentum.h"
#include "plist.h"
#include "xar.h"

nomen enumeratio {
    FASCICULUM_SUCCESSUS = ZEPHYRUM,
    FASCICULUM_ERROR_DESUNT,
    FASCICULUM_ERROR_EXSECUTABILE,
    FASCICULUM_ERROR_ICON,
    FASCICULUM_ERROR_PLISTA,
    FASCICULUM_ERROR_NON_FASCICULUM,
    FASCICULUM_ERROR_DIRECTORIUM,
    FASCICULUM_ERROR_SCRIPTIO,
    FASCICULUM_ERROR_MODUS,
    FASCICULUM_ERROR_MEMORIA
} FasciculumStatus;

nomen enumeratio {
    FASCICULUM_GENITUM,
    FASCICULUM_COPIATUM
} FasciculumOrigo;

/* Pars plani: aut OCTETI geniti, aut VIA fontis copiandi (par. III
 * specificationis: ergo reddere purum manet et binarium magnum per
 * piscinam non transit). */
nomen structura {
    FasciculumOrigo  origo;
             chorda  semita;   /* relativa: "Contents/Info.plist" */
             chorda  octeti;   /* GENITUM */
constans character*  fons;     /* COPIATUM */
                b32  exsecutabile_fiat;
} FasciculumPars;

nomen structura {
             chorda  identitas;  /* CFBundleIdentifier, necessarium */
             chorda  titulus;    /* CFBundleName */
             chorda  versio;     /* CFBundleShortVersionString */
             chorda  versio_aedificationis;  /* CFBundleVersion */
constans character*  exsecutabile;  /* via, necessarium */
constans character*  icon;          /* via ad .icns, aut NIHIL */
                b32  sine_scandali; /* LSUIElement */
        PlistValor*  plista_extra;  /* dictio fusa, aut NIHIL */
} Fasciculum;

/* Reddere planum: PURUM (nullum contentum de disco legit; vias fontium
 * solum EXSISTERE probat). partes = Xar de FasciculumPars.
 *
 * Redde FALSUM cum statu et sede vitii. */
b32
fasciculum_reddere (
    constans Fasciculum* f,
                   Xar** partes,
       FasciculumStatus* status,
                 chorda* sedes_vitii,
                Piscina* piscina);

/* Scribere planum sub radice data: directoria, plagulae genitae,
 * copiae, modus +x. SINE REVERSIONE: quod scriptum est manet, et
 * status semitam deficientem nominat (par. V). */
b32
fasciculum_scribere (
          constans Xar* partes,
    constans character* via_radicis,
       FasciculumStatus* status,
                 chorda* sedes_vitii,
                Piscina* piscina);

/* Legere fasciculum exsistentem. RECUSAT primum vitium nominans
 * (par. IV, VI); claves ignotae in plista_extra manent. */
b32
fasciculum_legere (
     constans character* via_radicis,
            Fasciculum* f,
       FasciculumStatus* status,
                 chorda* sedes_vitii,
                Piscina* piscina,
    InternamentumChorda* intern);

/* Aequalitas VALORUM (super plist_aequalis): ut scriptor fasciculum
 * iam rectum non rescribat. */
b32
fasciculum_aequalis (
    constans Fasciculum* a,
    constans Fasciculum* b);

#endif /* FASCICULUM_H */
EOF
```

- [ ] **Step 2: Write the failing tests (H1 + H2)**

```bash
./silva/scribe.sh probationes/probatio_fasciculum.c <<'EOF'
/* probatio_fasciculum.c - planum purum, scriptor, lector, iter reditus
 *
 * Portae H1-H6 (project-specs/fasciculum-spec.md par. VIII). Quaestio
 * quam nulla suita respondere potest - an fasciculus duplici ictu
 * aperiatur - in tools/fasciculum_fumus.sh manu vivit.
 */

#include "latina.h"
#include "fasciculum.h"
#include "filum.h"
#include "via.h"
#include "credo.h"
#include "piscina.h"
#include "internamentum.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Fasciculum minimum: exsecutabile verum in area, nihil aliud. */
interior Fasciculum
_fasciculum_minimum (
    constans character* via_exsecutabilis,
               Piscina* piscina)
{
    Fasciculum f;

    memset(&f, ZEPHYRUM, magnitudo(Fasciculum));
    f.identitas     = chorda_ex_literis("org.rhubarb.probatio",
                                        piscina);
    f.titulus       = chorda_ex_literis("Probatio", piscina);
    f.exsecutabile  = via_exsecutabilis;
    redde f;
}

s32
principale (vacuum)
{
                Piscina* piscina;
    InternamentumChorda* intern;
                    b32  praeteritus;
     constans character* via_binarii =
        "/tmp/probatio_fasciculum_binarium";

    piscina = piscina_generare_dynamicum("probatio_fasciculum",
                                         1048576);
    si (piscina == NIHIL)
    {
        imprimere("FRACTA: piscina\n");
        redde I;
    }
    intern = internamentum_creare(piscina);
    credo_aperire(piscina);

    /* exsecutabile fictum: reddere eum EXSISTERE poscit */
    CREDO_VERUM(filum_scribere_literis(via_binarii, "#!/bin/sh\n"));

    /* ---- H1: reddere PURUM ---- */
    {
              Fasciculum  f;
                    Xar*  partes;
        FasciculumStatus  status;
                  chorda  sedes;
         FasciculumPars*  pars;

        imprimere("\n--- H1: planum purum ---\n");

        f = _fasciculum_minimum(via_binarii, piscina);
        CREDO_VERUM(fasciculum_reddere(&f, &partes, &status, &sedes,
                                       piscina));
        CREDO_AEQUALIS_I32((i32)status, (i32)FASCICULUM_SUCCESSUS);
        CREDO_NON_NIHIL(partes);
        /* sine icona: Info.plist + exsecutabile = duae partes */
        CREDO_AEQUALIS_I32(xar_numerus(partes), II);

        pars = (FasciculumPars*)xar_obtinere(partes, ZEPHYRUM);
        CREDO_NON_NIHIL(pars);
        CREDO_AEQUALIS_I32((i32)pars->origo, (i32)FASCICULUM_GENITUM);
        CREDO_CHORDA_AEQUALIS_LITERIS(pars->semita,
                                      "Contents/Info.plist");
        CREDO_CHORDA_NON_VACUA(pars->octeti);
        CREDO_FALSUM(pars->exsecutabile_fiat);

        pars = (FasciculumPars*)xar_obtinere(partes, I);
        CREDO_AEQUALIS_I32((i32)pars->origo, (i32)FASCICULUM_COPIATUM);
        CREDO_CHORDA_AEQUALIS_LITERIS(pars->semita,
            "Contents/MacOS/probatio_fasciculum_binarium");
        CREDO_VERUM(pars->exsecutabile_fiat);
        CREDO_VERUM(pars->fons == via_binarii);

        /* PURITAS: nihil in disco creatum est */
        CREDO_FALSUM(filum_directorium_existit("Contents"));
    }

    /* ---- H2: claves Info.plist et directio fusionis ---- */
    {
              Fasciculum  f;
                    Xar*  partes;
        FasciculumStatus  status;
                  chorda  sedes;
         FasciculumPars*  pars;
           PlistResultus  r;
             PlistValor*  v;

        imprimere("\n--- H2: claves et fusio ---\n");

        f = _fasciculum_minimum(via_binarii, piscina);
        f.versio        = chorda_ex_literis("2.5", piscina);
        f.sine_scandali = VERUM;
        f.plista_extra  = plist_dictio_creare(piscina);
        /* clavis generata SUBSTITUITUR a vocante (D8) */
        plist_dictio_ponere(f.plista_extra,
            chorda_ex_literis("CFBundlePackageType", piscina),
            plist_chordam_creare(chorda_ex_literis("BNDL", piscina),
                                 piscina), piscina);
        plist_dictio_ponere(f.plista_extra,
            chorda_ex_literis("NSAppleScriptEnabled", piscina),
            plist_veritatem_creare(VERUM, piscina), piscina);

        CREDO_VERUM(fasciculum_reddere(&f, &partes, &status, &sedes,
                                       piscina));
        pars = (FasciculumPars*)xar_obtinere(partes, ZEPHYRUM);
        r = plist_legere(pars->octeti, piscina, intern);
        CREDO_AEQUALIS_I32((i32)r.status, (i32)PLIST_SUCCESSUS);

        v = plist_dictio_capere(r.radix, "CFBundleIdentifier");
        CREDO_CHORDA_AEQUALIS_LITERIS(v->textus,
                                      "org.rhubarb.probatio");
        /* DERIVATA, non data */
        v = plist_dictio_capere(r.radix, "CFBundleExecutable");
        CREDO_CHORDA_AEQUALIS_LITERIS(v->textus,
            "probatio_fasciculum_binarium");
        v = plist_dictio_capere(r.radix, "CFBundleShortVersionString");
        CREDO_CHORDA_AEQUALIS_LITERIS(v->textus, "2.5");
        /* praesto SEMPER */
        v = plist_dictio_capere(r.radix, "NSHighResolutionCapable");
        CREDO_NON_NIHIL(v);
        CREDO_VERUM(v->veritas);
        v = plist_dictio_capere(r.radix, "LSUIElement");
        CREDO_NON_NIHIL(v);
        /* FUSIO: vocans vincit */
        v = plist_dictio_capere(r.radix, "CFBundlePackageType");
        CREDO_CHORDA_AEQUALIS_LITERIS(v->textus, "BNDL");
        v = plist_dictio_capere(r.radix, "NSAppleScriptEnabled");
        CREDO_NON_NIHIL(v);
    }

    filum_delere(via_binarii);

    imprimere("\n");
    credo_imprimere_compendium();
    praeteritus = credo_omnia_praeterierunt();
    credo_claudere();
    piscina_destruere(piscina);

    redde praeteritus ? ZEPHYRUM : I;
}
EOF
```

- [ ] **Step 3: Run and watch it fail (link, not syntax)**

```bash
./compile_tests.sh fasciculum > /tmp/f2.log 2>&1; echo "rc=$?"
grep -E 'error:' /tmp/f2.log | head -4
grep -E 'Undefined symbols|_fasciculum_' /tmp/f2.log | head -6
```

Expected: no compile errors, undefined `_fasciculum_reddere`. If a
compile error appears instead, suspect a `latina.h` macro in a field
name FIRST (that trap bit twice in plist's arc).

- [ ] **Step 4: Implement `lib/fasciculum.c` — render only**

```bash
./silva/scribe.sh lib/fasciculum.c <<'EOF'
/* fasciculum.c - fasciculi .app (contractus in capite).
 *
 * Info.plist dictio PlistValor est quam plist_scribere serializat;
 * reddere nihil de disco legit (vias solum exsistere probat), ergo
 * dispositio tota sine systemate plagularum probari potest.
 */

#include "fasciculum.h"
#include "filum.h"
#include "via.h"
#include <string.h>

interior b32
_recusare (
    FasciculumStatus* status,
              chorda* sedes_vitii,
    FasciculumStatus  valor,
              chorda  sedes)
{
    *status       = valor;
    *sedes_vitii  = sedes;
    redde FALSUM;
}

/* clavem ponere NISI vocans eam in plista_extra iam posuit (D8:
 * plista_extra vincit) */
interior vacuum
_clavem_ponere (
     PlistValor* dictio,
     PlistValor* extra,
constans character* clavis,
     PlistValor* valor,
       Piscina* piscina)
{
    si (extra && plist_dictio_capere(extra, clavis))
    {
        redde;
    }
    plist_dictio_ponere(dictio, chorda_ex_literis(clavis, piscina),
                        valor, piscina);
}

interior chorda
_semita_iuncta (
    constans character* a,
                 chorda b,
               Piscina* piscina)
{
    chorda partes[II];

    partes[0] = chorda_ex_literis(a, piscina);
    partes[1] = b;
    redde via_iungere(partes, II, piscina);
}

b32
fasciculum_reddere (
    constans Fasciculum* f,
                   Xar** partes,
       FasciculumStatus* status,
                 chorda* sedes_vitii,
                Piscina* piscina)
{
         PlistValor* dictio;
         PlistStatus plista_status;
              chorda nomen_exsecutabilis;
              chorda octeti;
              chorda vacua;
     FasciculumPars* pars;
         FilumStatus st;

    vacua.datum    = NIHIL;
    vacua.mensura  = ZEPHYRUM;
    *status        = FASCICULUM_SUCCESSUS;
    *sedes_vitii   = vacua;

    si (!f || !partes || !piscina)
    {
        redde _recusare(status, sedes_vitii, FASCICULUM_ERROR_DESUNT,
                        vacua);
    }
    si (chorda_vacua(f->identitas) || !f->exsecutabile)
    {
        redde _recusare(status, sedes_vitii, FASCICULUM_ERROR_DESUNT,
                        f->identitas);
    }
    si (!filum_status(f->exsecutabile, &st) || !st.est_filum)
    {
        redde _recusare(status, sedes_vitii,
                        FASCICULUM_ERROR_EXSECUTABILE,
                        chorda_ex_literis(f->exsecutabile, piscina));
    }
    si (f->icon && (!filum_status(f->icon, &st) || !st.est_filum))
    {
        redde _recusare(status, sedes_vitii, FASCICULUM_ERROR_ICON,
                        chorda_ex_literis(f->icon, piscina));
    }

    nomen_exsecutabilis = via_nomen(
        chorda_ex_literis(f->exsecutabile, piscina), piscina);

    /* ---- Info.plist ---- */
    dictio = plist_dictio_creare(piscina);
    si (!dictio)
    {
        redde _recusare(status, sedes_vitii, FASCICULUM_ERROR_MEMORIA,
                        vacua);
    }
    _clavem_ponere(dictio, f->plista_extra, "CFBundleIdentifier",
        plist_chordam_creare(f->identitas, piscina), piscina);
    _clavem_ponere(dictio, f->plista_extra, "CFBundleExecutable",
        plist_chordam_creare(nomen_exsecutabilis, piscina), piscina);
    _clavem_ponere(dictio, f->plista_extra, "CFBundleName",
        plist_chordam_creare(chorda_vacua(f->titulus)
            ? nomen_exsecutabilis : f->titulus, piscina), piscina);
    _clavem_ponere(dictio, f->plista_extra, "CFBundlePackageType",
        plist_chordam_creare(chorda_ex_literis("APPL", piscina),
                             piscina), piscina);
    _clavem_ponere(dictio, f->plista_extra,
        "CFBundleInfoDictionaryVersion",
        plist_chordam_creare(chorda_ex_literis("6.0", piscina),
                             piscina), piscina);
    _clavem_ponere(dictio, f->plista_extra,
        "CFBundleShortVersionString",
        plist_chordam_creare(chorda_vacua(f->versio)
            ? chorda_ex_literis("1.0", piscina) : f->versio, piscina),
        piscina);
    _clavem_ponere(dictio, f->plista_extra, "CFBundleVersion",
        plist_chordam_creare(chorda_vacua(f->versio_aedificationis)
            ? chorda_ex_literis("1", piscina)
            : f->versio_aedificationis, piscina), piscina);
    si (f->icon)
    {
        _clavem_ponere(dictio, f->plista_extra, "CFBundleIconFile",
            plist_chordam_creare(via_nomen(
                chorda_ex_literis(f->icon, piscina), piscina),
                piscina), piscina);
    }
    si (f->sine_scandali)
    {
        _clavem_ponere(dictio, f->plista_extra, "LSUIElement",
            plist_veritatem_creare(VERUM, piscina), piscina);
    }
    _clavem_ponere(dictio, f->plista_extra, "NSHighResolutionCapable",
        plist_veritatem_creare(VERUM, piscina), piscina);

    /* claves vocantis: omnes, etiam generatas substituentes */
    si (f->plista_extra)
    {
        i32 i;

        per (i = ZEPHYRUM; i < plist_numerus(f->plista_extra); i++)
        {
            PlistPar* par = (PlistPar*)xar_obtinere(
                f->plista_extra->liberi, i);

            si (par)
            {
                plist_dictio_ponere(dictio, par->clavis, par->valor,
                                    piscina);
            }
        }
    }

    octeti = plist_scribere(dictio, &plista_status, piscina);
    si (plista_status != PLIST_SUCCESSUS || chorda_vacua(octeti))
    {
        redde _recusare(status, sedes_vitii, FASCICULUM_ERROR_PLISTA,
                        chorda_ex_literis("Contents/Info.plist",
                                          piscina));
    }

    /* ---- planum ---- */
    *partes = xar_creare(piscina, (i32)magnitudo(FasciculumPars));
    si (!*partes)
    {
        redde _recusare(status, sedes_vitii, FASCICULUM_ERROR_MEMORIA,
                        vacua);
    }

    pars = (FasciculumPars*)xar_addere(*partes);
    pars->origo               = FASCICULUM_GENITUM;
    pars->semita              = chorda_ex_literis(
        "Contents/Info.plist", piscina);
    pars->octeti              = octeti;
    pars->fons                = NIHIL;
    pars->exsecutabile_fiat   = FALSUM;

    pars = (FasciculumPars*)xar_addere(*partes);
    pars->origo               = FASCICULUM_COPIATUM;
    pars->semita              = _semita_iuncta("Contents/MacOS",
        nomen_exsecutabilis, piscina);
    pars->octeti              = vacua;
    pars->fons                = f->exsecutabile;
    pars->exsecutabile_fiat   = VERUM;

    si (f->icon)
    {
        pars = (FasciculumPars*)xar_addere(*partes);
        pars->origo             = FASCICULUM_COPIATUM;
        pars->semita            = _semita_iuncta("Contents/Resources",
            via_nomen(chorda_ex_literis(f->icon, piscina), piscina),
            piscina);
        pars->octeti            = vacua;
        pars->fons              = f->icon;
        pars->exsecutabile_fiat = FALSUM;
    }
    redde VERUM;
}
EOF
```

- [ ] **Step 5: Wire the manifest, run, watch it pass**

```bash
./tools/compile_tests_fontes_generare.sh
./compile_tests.sh fasciculum > /tmp/f2.log 2>&1; echo "rc=$?"
grep -E 'H1|H2|Totalis|Praeteriti|Fracti|Conditio' /tmp/f2.log
```

- [ ] **Step 6: Two plants (sequentially, never batched)**

```bash
python3 - <<'PY'
import sys; sys.path.insert(0, 'pythonica'); import silva
# H1: exsecutabile non iam +x notatur
print(silva.planta('lib/fasciculum.c',
                   'pars->exsecutabile_fiat   = VERUM;',
                   'pars->exsecutabile_fiat   = FALSUM;',
                   'radix', 'fasciculum'))
PY
python3 - <<'PY'
import sys; sys.path.insert(0, 'pythonica'); import silva
# H2: fusio inversa - claves generatae vocantem evertunt
print(silva.planta('lib/fasciculum.c',
                   '''    si (extra && plist_dictio_capere(extra, clavis))
    {
        redde;
    }''',
                   '''    si (extra && FALSUM)
    {
        redde;
    }'''))
PY
```

The second plant needs the loop that re-applies `plista_extra` removed
as well, or it stays green — if it does, that is the plan telling you
the merge is asserted in only one direction; add the missing assertion
rather than weakening the plant.

- [ ] **Step 7: Lint, format, commit**

```bash
./oratio/vocabula.sh -nova
./silva/formator.sh include/fasciculum.h lib/fasciculum.c \
    probationes/probatio_fasciculum.c -scribere
./silva/formator.sh include/fasciculum.h lib/fasciculum.c \
    probationes/probatio_fasciculum.c -vitia
```

Any new word: add a glossary entry for a real term, rename only a bad
name. Then `silva.commissio` with
`portae=[('radix', 'fasciculum')]`, listing the three new files plus
`compile_tests_fontes_generata.sh`.

---

### Task 3: the writer

Implements spec §5; gates H4 and H6.

**Files:** modify `lib/fasciculum.c`, `probationes/probatio_fasciculum.c`

**Interfaces:**
- Consumes: task 1's `filum_modum_ponere` and `potest_exsequi`; task 2's
  plan.
- Produces: `fasciculum_scribere`.

- [ ] **Step 1: Write the failing tests (H4 + H6)**

Insert before the compendium pair. H6 restores the directory mode in
every exit path — an unwritable directory left in `/tmp` breaks the
NEXT run, which is the shared-fixture hazard that has bitten this repo
before.

```c
    /* ---- H4: scriptor in area ---- */
    {
              Fasciculum  f;
                    Xar*  partes;
        FasciculumStatus  status;
                  chorda  sedes;
             FilumStatus  st;
               character  area[CCLVI];
               character  via_plistae[CCLVI];
               character  via_exs[CCLVI];

        imprimere("\n--- H4: scriptor ---\n");

        sprintf(area, "/tmp/probatio_fasciculum_area/Probatio.app");
        f = _fasciculum_minimum(via_binarii, piscina);
        CREDO_VERUM(fasciculum_reddere(&f, &partes, &status, &sedes,
                                       piscina));
        CREDO_VERUM(fasciculum_scribere(partes, area, &status, &sedes,
                                        piscina));
        CREDO_AEQUALIS_I32((i32)status, (i32)FASCICULUM_SUCCESSUS);

        sprintf(via_plistae, "%.200s/Contents/Info.plist", area);
        sprintf(via_exs,
            "%.200s/Contents/MacOS/probatio_fasciculum_binarium",
            area);
        CREDO_VERUM(filum_existit(via_plistae));
        CREDO_VERUM(filum_status(via_exs, &st));
        CREDO_VERUM(st.est_filum);
        /* BIT EXSEQUENDI: causa paris in filum (opus I) */
        CREDO_VERUM(st.potest_exsequi);
    }

    /* ---- H6: scriptio semiperfecta status semitam nominat ---- */
    {
              Fasciculum  f;
                    Xar*  partes;
        FasciculumStatus  status;
                  chorda  sedes;
               character  area[CCLVI];
               character  via_macos[CCLVI];
               character  via_plistae[CCLVI];

        imprimere("\n--- H6: scriptio semiperfecta ---\n");

        sprintf(area, "/tmp/probatio_fasciculum_clausa/Probatio.app");
        sprintf(via_macos, "%.200s/Contents/MacOS", area);
        sprintf(via_plistae, "%.200s/Contents/Info.plist", area);

        f = _fasciculum_minimum(via_binarii, piscina);
        CREDO_VERUM(fasciculum_reddere(&f, &partes, &status, &sedes,
                                       piscina));
        /* Contents/MacOS praestruitur et CLAUDITUR */
        CREDO_VERUM(filum_directorium_creare_si_necesse(via_macos));
        CREDO_VERUM(filum_modum_ponere(via_macos, 0500));

        CREDO_FALSUM(fasciculum_scribere(partes, area, &status, &sedes,
                                         piscina));
        CREDO_VERUM(status == FASCICULUM_ERROR_SCRIPTIO
                 || status == FASCICULUM_ERROR_MODUS);
        CREDO_CHORDA_CONTINET(sedes,
            chorda_ex_literis("Contents/MacOS", piscina));
        /* SINE REVERSIONE: plista iam scripta MANET */
        CREDO_VERUM(filum_existit(via_plistae));

        /* modum RESTITUE semper: aliter cursus proximus haeret */
        CREDO_VERUM(filum_modum_ponere(via_macos, 0755));
    }
```

- [ ] **Step 2: Run; expect the link to fail on `fasciculum_scribere`**

```bash
./compile_tests.sh fasciculum > /tmp/f3.log 2>&1; echo "rc=$?"
grep -E '_fasciculum_scribere' /tmp/f3.log | head -3
```

- [ ] **Step 3: Implement**

Append to `lib/fasciculum.c`:

```c
b32
fasciculum_scribere (
          constans Xar* partes,
    constans character* via_radicis,
       FasciculumStatus* status,
                 chorda* sedes_vitii,
                Piscina* piscina)
{
    chorda vacua;
       i32 i;

    vacua.datum    = NIHIL;
    vacua.mensura  = ZEPHYRUM;
    *status        = FASCICULUM_SUCCESSUS;
    *sedes_vitii   = vacua;

    si (!partes || !via_radicis || !piscina)
    {
        redde _recusare(status, sedes_vitii, FASCICULUM_ERROR_DESUNT,
                        vacua);
    }
    per (i = ZEPHYRUM; i < xar_numerus(partes); i++)
    {
        FasciculumPars* pars;
                chorda  absoluta;
            character*  via_c;
                chorda  directorium;

        pars = (FasciculumPars*)xar_obtinere(partes, i);
        si (!pars)
        {
            redde _recusare(status, sedes_vitii,
                            FASCICULUM_ERROR_MEMORIA, vacua);
        }
        absoluta     = _semita_iuncta(via_radicis, pars->semita,
                                      piscina);
        via_c        = chorda_ut_cstr(absoluta, piscina);
        directorium  = via_directorium(absoluta, piscina);

        si (!filum_directorium_creare_si_necesse(
                chorda_ut_cstr(directorium, piscina)))
        {
            redde _recusare(status, sedes_vitii,
                            FASCICULUM_ERROR_DIRECTORIUM, directorium);
        }
        si (pars->origo == FASCICULUM_GENITUM)
        {
            si (!filum_scribere(via_c, pars->octeti))
            {
                redde _recusare(status, sedes_vitii,
                                FASCICULUM_ERROR_SCRIPTIO,
                                pars->semita);
            }
        }
        alioquin
        {
            si (!pars->fons || !filum_copiare(pars->fons, via_c))
            {
                redde _recusare(status, sedes_vitii,
                                FASCICULUM_ERROR_SCRIPTIO,
                                pars->semita);
            }
        }
        si (pars->exsecutabile_fiat
            && !filum_modum_ponere(via_c, 0755))
        {
            redde _recusare(status, sedes_vitii,
                            FASCICULUM_ERROR_MODUS, pars->semita);
        }
    }
    redde VERUM;
}
```

- [ ] **Step 4: Run green, then two plants (sequentially)**

Plant one: drop the `exsecutabile_fiat` chmod call — H4's
`potest_exsequi` assertion must go red. Plant two: return VERUM instead
of refusing when `filum_scribere` fails — H6's status assertion must go
red. Each: plant, run the gate, revert, DELETE
`build/probationes/probatio_fasciculum`, re-run.

- [ ] **Step 5: Whole suite, format, commit** (`portae=[('radix',
  'fasciculum')]`).

---

### Task 4: the reader and the comparison

Implements spec §4's inverse and §6; gates H3 and H5.

**Files:** modify `lib/fasciculum.c`, `probationes/probatio_fasciculum.c`

**Interfaces:**
- Consumes: tasks 2 and 3.
- Produces: `fasciculum_legere`, `fasciculum_aequalis`.

- [ ] **Step 1: Write the failing tests (H3 + H5)**

H3 walks every status in §6 with its own case: a value missing
`identitas`; an `exsecutabile` path that does not exist; an `icon` that
does not exist; a directory with no `Contents/`; a bundle whose
`Info.plist` is malformed (assert `FASCICULUM_ERROR_PLISTA` AND that
plist's own status travelled); a bundle whose executable named by
`CFBundleExecutable` is absent. Each asserts the status AND that
`sedes_vitii` names the offending path.

H5 writes a bundle with an icon, reads it back, and asserts
`fasciculum_aequalis` against the original value; then removes
`Contents/MacOS/<exe>` and asserts the read refuses.

- [ ] **Step 2: Run; expect undefined `_fasciculum_legere`.**

- [ ] **Step 3: Implement `fasciculum_legere`**

Read `Contents/Info.plist` with `plist_legere_plagulam`; on refusal
return `FASCICULUM_ERROR_PLISTA` carrying plist's `semita` as
`sedes_vitii`. Lift the ten keys of spec §4 into the value; every other
pair goes into a fresh `plista_extra` dict. Confirm
`Contents/MacOS/<CFBundleExecutable>` with `filum_status`, else
`FASCICULUM_ERROR_EXSECUTABILE`. `Contents/` absent is
`FASCICULUM_ERROR_NON_FASCICULUM`.

`fasciculum_aequalis` compares the scalar fields with
`chorda_aequalis`, the executable and icon basenames (not full paths —
a bundle read back knows only its own names), and `plista_extra` with
`plist_aequalis`.

- [ ] **Step 4: Run green; two plants** — `legere` not checking the
  executable's existence (H5's refusal goes red), and `aequalis`
  ignoring `plista_extra` (H5's round trip goes red).

- [ ] **Step 5: Whole suite, format, commit.**

---

### Task 5: the by-hand smoke, and the seal

**Files:** create `tools/fasciculum_fumus.sh`, `lib/fasciculum.worklog.md`;
modify `project-specs/fasciculum-spec.md`

- [ ] **Step 1: Write `tools/fasciculum_fumus.sh`**

Modeled on `tools/briar_fumus.sh`. It builds a real bundle in a
`mktemp -d` area around a real binary (`bin/moneta_probatio` or any
small built tool), runs `plutil -lint` on the generated plist, prints
the bundle's tree, and then STOPS with an instruction: `open <area>/
Probatio.app` — because launching is a GUI act. `-agere` opens it.
Exit 2 when prerequisites are missing; never called by the suite.

- [ ] **Step 2: Run it by hand and record what happened**

This is the AUDIENDUM the spec names. Whatever it reports — launches,
bounces in the Dock, refuses with a Gatekeeper message — goes in the
worklog verbatim. If the bundle does NOT launch, that is the plan's
most valuable finding and the fix belongs before the seal.

- [ ] **Step 3: Worklog and spec as-built**

Record, in English: the two `filum` rows and why they existed; whether
the merge direction survived contact; what the by-hand smoke said; and
any step of this plan that was wrong (plist's plan had five — expect
some).

- [ ] **Step 4: Whole root suite as the acceptance gate**

```bash
./compile_tests.sh > /tmp/omnia.log 2>&1; echo "rc=$?"
grep -E 'Tests (Total|Passed|Failed)' /tmp/omnia.log
./oratio/vocabula.sh -nova
```

- [ ] **Step 5: Commit the seal** (`portae=[('radix', None)]`).

---

## Self-review (done while writing; the executor re-runs it at the end)

**Spec coverage.** §3 → task 2 steps 1, 4. §4 → task 2 (generation) and
task 4 (the inverse). §5 → task 3. §6 → task 4's H3. §7.1, §7.2 → task
1. §8 H1, H2 → task 2; H4, H6 → task 3; H3, H5 → task 4. §9 P1–P5 →
tasks 1–5. §10 deferrals: nothing here implements them, as intended.
§11's AUDIENDUM about launching → task 5 step 2, which is the only step
whose outcome this plan cannot predict.

**Placeholders.** Tasks 1–3 carry full code. Tasks 4 and 5 are
described rather than written out: the reader is the writer's inverse
key by key, and writing 200 lines of it here would duplicate spec §4
without adding information. That is a deliberate thinness, and it is
the one place an executor may need to think rather than transcribe — if
that proves wrong in practice, the fix is to write task 4's code out in
full on the next pass.

**Type consistency.** `FasciculumStatus` is the status type in all four
functions; `sedes_vitii` is a `chorda*` everywhere; `partes` is `Xar**`
out / `constans Xar*` in, matching the house convention at
`include/aedilis.h:42-43`. `_semita_iuncta` is used by both `reddere`
and `scribere` with the same argument order. `0755` appears as the one
executable mode, in task 1's test, task 3's writer, and H6's restore.

**The one risk was checked before this plan was committed**, so the
executor inherits facts:

- `via_iungere` (`lib/via.c:72-128`) concatenates parts, inserting a
  separator only when neither side already carries one and skipping
  empty parts. It performs NO normalization, so
  `via_iungere({"…/Probatio.app", "Contents/MacOS/exe"})` yields exactly
  that nested relative path — the paths H1 asserts are literal, and
  `_semita_iuncta` needs no `chorda_aedificator` fallback.
- `via_directorium` (`:276-305`) returns everything before the last
  separator, `"."` when there is none, `"/"` at the root — which is what
  `fasciculum_scribere` needs to create `Contents/MacOS` before writing
  into it.

What remains genuinely unknown is **task 5 step 2**: whether a bundle
this library assembles actually launches. No step before it can answer
that, and if it does not launch, that finding outranks the seal.
