/* Phase 1 test: ID ID patterns (two identifiers in sequence) */

/* Simple typedef-style declaration: MyType myVar */
MyType myVar;

/* With pointer: MyType* myPtr */
MyType* myPtr;

/* Function declaration: MyType myFunc(void) */
MyType myFunc(void);

/* Multiple declarators on same type */
MyType a, b, c;
